{#
/**
 * @file
 * Theme override to display a single page.
 *
 * The doctype, html, head and body tags are not in this template. Instead they
 * can be found in the html.html.twig template in this directory.
 *
 * Available variables:
 *
 * General utility variables:
 * - base_path: The base URL path of the Drupal installation. Will usually be
 *   "/" unless you have installed Drupal in a sub-directory.
 * - is_front: A flag indicating if the current page is the front page.
 * - logged_in: A flag indicating if the user is registered and signed in.
 * - is_admin: A flag indicating if the user has permission to access
 *   administration pages.
 *
 * Site identity:
 * - front_page: The URL of the front page. Use this instead of base_path when
 *   linking to the front page. This includes the language domain or prefix.
 *
 * Page content (in order of occurrence in the default page.html.twig):
 * - node: Fully loaded node, if there is an automatically-loaded node
 *   associated with the page and the node ID is the second argument in the
 *   page's path (e.g. node/12345 and node/12345/revisions, but not
 *   comment/reply/12345).
 *
 * Rows:
 * - row.[row_name].attributes: Attributes for the current row.
 *   @see zero_preprocess_page()
 *   @see active_regions()
 *
 * Containers:
 * - container.attributes: Attributes for the containers.
 *
 * Regions:
 * - page.leaderboard: Items for the leaderboard region.
 * - page.branding: Items for the branding region.
 * - page.navbar: Items for the navbar region.
 * - page.header: Items for the header region.
 * - page.breadcrumb: Items for the breadcrumb region.
 * - page.highlighted: Items for the highlighted region.
 * - page.features: Items for the features region.
 * - page.content_prefix: Items for the content prefix region.
 * - page.content: The main content of the current page.
 * - page.content_suffix: Items for the content suffix region.
 * - page.sidebar_first: Items for the first sidebar.
 * - page.sidebar_second: Items for the second sidebar.
 * - page.subfeatures: Items for the subfeatures region.
 * - page.footer_prefix: Items for the footer prefix region.
 * - page.footer: Items for the footer region.
 * - page.footer_suffix: Items for the footer suffix region.
 *
 * @see zero_preprocess_page()
 * @see html.html.twig
 */
#}
<div class="wrapper wrapper-page">
  {% if page.leaderboard %}
    <div class="row row-leaderboard">
      <div{{ container.attributes }}>
        {{ page.leaderboard }}
      </div>
    </div>
  {% endif %}
  <header class="wrapper wrapper-header" role="banner" aria-label="{{ 'Site header'| t }}">
    <div class="row row-navbar"{{ droppy_attributes }}>
      <div{{ container.attributes }}>
        {{ page.branding }}
        <div class="wrapper wrapper-navbar">
          {% if adaptive_navbar %}
            {% if use_svg_icon %}
              <button class="trigger trigger-navbar">
                {{ trigger.icon }}
              </button>
            {% else %}
              <button class="trigger trigger-navbar">
                <span>{{ trigger.label }}</span>
              </button>
            {% endif %}
          {% endif %}
          {{ page.navbar }}
        </div>
      </div>
    </div>
    {% if page.header %}
      <div class="row row-header">
        <div{{ container.attributes }}>
          {{ page.header }}
        </div>
      </div>
    {% endif %}
  </header>
  {% if page.breadcrumb %}
    <div class="row row-breadcrumb">
      <div{{ container.attributes }}>
        {{ page.breadcrumb }}
      </div>
    </div>
  {% endif %}
  {% if page.highlighted %}
    <div class="row row-highlighted">
      <div{{ container.attributes }}>
        {{ page.highlighted }}
      </div>
    </div>
  {% endif %}
  {% if page.features %}
    <div class="row row-features">
      <div{{ container.attributes }}>
        {{ page.features }}
      </div>
    </div>
  {% endif %}
  {% if page.content_prefix %}
    <div class="row row-content-prefix">
      <div{{ container.attributes }}>
        {{ page.content_prefix }}
      </div>
    </div>
  {% endif %}
  <div{{ row.main.attributes.addClass('row', 'row-main') }}>
    <div{{ container.attributes }}>
      {{ page.content }}
      {% if page.sidebar_first %}
        {{ page.sidebar_first }}
      {% endif %}
      {% if page.sidebar_second %}
        {{ page.sidebar_second }}
      {% endif %}
    </div>
  </div>
  {% if page.content_suffix %}
    <div class="row row-content-suffix">
      <div{{ container.attributes }}>
        {{ page.content_suffix }}
      </div>
    </div>
  {% endif %}
  {% if page.subfeatures %}
    <div class="row row-subfeatures">
      <div{{ container.attributes }}>
        {{ page.subfeatures }}
      </div>
    </div>
  {% endif %}
  <footer class="wrapper wrapper-footer">
    {% if page.footer_prefix %}
      <div class="row row-footer-prefix">
        <div{{ container.attributes }}>
          {{ page.footer_prefix }}
        </div>
      </div>
    {% endif %}
    <div class="row row-footer">
      <div{{ container.attributes }}>
        {{ page.footer }}
      </div>
    </div>
    {% if page.footer_suffix %}
      <div class="row row-footer-prefix">
        <div{{ container.attributes }}>
          {{ page.footer_suffix }}
        </div>
      </div>
    {% endif %}
  </footer>
</div>